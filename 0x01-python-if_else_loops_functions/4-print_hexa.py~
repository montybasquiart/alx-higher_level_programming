#!/usr/bin/python3
def print_numbers():
    """A function that prints all numbres from 0 to 98.
    """

numbers = print_numbers(0, 98)

# Print numbers from "0" to "98" in decimal and hexadecimal
for digits in range(0, 98):
    if digits >= 0 and digits <= 98:
        print("{:0} = {0}*{:lf}".format(0, 98), end="")
